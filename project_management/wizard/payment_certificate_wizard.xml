<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_payment_certificate_wizard" model="ir.ui.view">
        <field name="name">payment.certificate.wizard.wizard.form</field>
        <field name="model">payment.certificate.wizard</field>
        <field name="arch" type="xml">
            <form string="Payment Certificate">
                <group>
                    <field name="project_id" invisible="1"/>

                    <field name="requested_percentage" required="1"/>
                </group>
                <footer>
                    <button string="Create" type="object" name="action_confirm" class="btn-primary"/>
                    <button string="Cancel" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_payment_certificate_wizard_wizard" model="ir.actions.act_window">
        <field name="name">Payment Certificate</field>
        <field name="res_model">payment.certificate.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_payment_certificate_wizard"/>
        <field name="target">new</field>
        <field name="context"></field>
    </record>
</odoo>
